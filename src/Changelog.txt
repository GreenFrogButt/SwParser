Note to self:  When making a release
    1)  Update versionString in SwParser
    2)  Copy the jar file to src

Made javaBuffoonary an enum.

saveDatabase no longer redirects stdout, prints directly to a file instead.

World.parseDepartedFleets does the same thing as MovedFleets.parseMovedFleets.  Oops.

MovedFleets.kt now uses matched groups.